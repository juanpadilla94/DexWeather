package com.example.dexweather;

import org.json.JSONException;
import org.junit.Assert;
import org.junit.Test;

import java.lang.reflect.InvocationTargetException;
import java.util.concurrent.ExecutionException;

import static org.junit.Assert.*;

public class WeatherUnitTest {
    /*
    @Test
    public void weatherExecution() throws NoSuchMethodException, InvocationTargetException, IllegalAccessException {
        try
        {
            MainActivity.class.getDeclaredMethod("getWeather").invoke(null);
            assertTrue(true);
        }
        catch(ExecutionException e)
        {
            assertTrue(false);
        }
    }

    @Test
    public void weatherJSON() throws NoSuchMethodException, InvocationTargetException, IllegalAccessException {
        try
        {
            MainActivity.class.getDeclaredMethod("getWeather").invoke(null);
            assertTrue(true);
        }
        catch(JSONException e)
        {
            assertTrue(false);
        }
    }
    */
}